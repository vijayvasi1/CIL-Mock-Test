import { Question } from "../types";
import { ALL_SETS } from "./allSetsData";

// Attach the bundled initial dataset
export function initializeBundledSets() {
  // All sets are pre-initialized in ALL_SETS
}

