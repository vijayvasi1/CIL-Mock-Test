import { Question } from "../types";
import { ALL_SETS } from "./allSetsData";

export const INITIAL_RAW_DATA: Record<string, Question[]> = ALL_SETS;

// Initialize all sets
export function initAllQuizData() {
  // Sets are pre-populated in ALL_SETS
}

